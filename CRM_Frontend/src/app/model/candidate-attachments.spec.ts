import { CandidateAttachments } from './candidate-attachments';

describe('CandidateAttachments', () => {
  it('should create an instance', () => {
    expect(new CandidateAttachments()).toBeTruthy();
  });
});
